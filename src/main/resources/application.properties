spring.application.name=sheprenure
server.forward-headers-strategy=framework

# Database configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/sheprenure}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:sarath 1577 7999}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=true

# Brevo (Sendinblue) Email API configuration (Works seamlessly on Render Free Tier via HTTPS)
# IMPORTANT: Use an HTTP API key (starts with 'xkeysib-'), NOT an SMTP key (starts with 'xsmtpsib-')
brevo.api-key=${BREVO_API_KEY:}
brevo.sender-email=${BREVO_SENDER_EMAIL:sarathdasika@gmail.com}
brevo.sender-name=${BREVO_SENDER_NAME:SHEPRENURE}

# SMTP Mail configuration (Gmail / Brevo SMTP fallback)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:sarathdasika@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:obrkornpctwhnima}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Cloudinary configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:pmcsctxr}
cloudinary.api-key=${CLOUDINARY_API_KEY:235767187546698}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:rYzIb-eMWYtVtAZvCp_IuWa_OdM}

# JWT secret key
jwt.secret=${JWT_SECRET:sheprenurekbncollegevijayawadaonetowngudivadakakaraparthibhavanarayanakalasala}

# Google OAuth2 configuration
# You can set GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET as env vars or configure them here
google.client-id=${GOOGLE_CLIENT_ID:}
google.client-secret=${GOOGLE_CLIENT_SECRET:}

# Frontend client URL
frontend.url=${FRONTEND_URL:http://localhost:5173}